import {Button} from '@itsjonq/react-select';
import {PropTable} from '../../components/PropTable';

# Button

A button!

Here's an example:

<Button>Buy now!</Button>

<PropTable component={Button} />
